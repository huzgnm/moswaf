# Holds the build output of web/ (Vite writes here so Go can embed it).
# The build is never committed; this file only keeps the directory present so
# `go build` always has something to embed.
*
!.gitignore
